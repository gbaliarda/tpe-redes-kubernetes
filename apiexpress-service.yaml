apiVersion: v1
kind: Service
metadata:
  name: apiexpress-service
  labels:
    app: apiexpress-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: apiexpress